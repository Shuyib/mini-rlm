name: nbdev-prepare

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  nbdev-prepare:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install nbdev

      - name: Ensure nbs metadata exists
        run: |
          # ensure nbs directory exists and nbdev.yml is present to avoid nbdev errors
          mkdir -p nbs
          if [ ! -f nbs/nbdev.yml ] && [ -f nbs/nbdev.yml.example ]; then
            cp nbs/nbdev.yml.example nbs/nbdev.yml
          fi

      - name: Run nbdev-prepare
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          nbdev-prepare
